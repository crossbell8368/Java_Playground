< 코드 빈칸에 들어갈 내용으로 적절한 것은? >

while(l < r) {
    int m = (l + r) / 2;
    if(arr[m] < x){
        l = m + 1;
    } else {
        r = m;
    }
    return l;
}

=> r을 답의 후보로 포함하지 않은 이유는, r이 범위값에 들어가기에..?
=> 그럼으로 l은 기존과 같게, r은 m값 그대로..?